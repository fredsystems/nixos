#!/usr/bin/env zsh
# shellcheck shell=bash
# shellcheck disable=SC1091
#
# Thin wrapper: shared functions live in dotfiles/shell/40-functions.sh so
# bash and zsh share one source of truth.
source "${HOME}/.config/shell/40-functions.sh"
